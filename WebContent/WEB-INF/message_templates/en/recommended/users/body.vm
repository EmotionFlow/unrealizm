#parse("en/header.vm")

あなたがUnrealizmでフォローしていないおすすめユーザーを紹介します。
Here are some recommended users that you are not following on Unrealizm.

#foreach($u in $recommend_users)
[$u.nickname]
#if($u.profile.length()>0)
$u.profile
#end
https://unrealizm.com/$u.userId

#end

Unrealizmのフォローは相手に伝わりません。気軽にフォローしてみましょう。
Following by Unrealizm is not notify to the other person. Feel free to follow them.

#parse("en/footer.vm")
