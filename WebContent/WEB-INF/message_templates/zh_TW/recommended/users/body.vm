#parse("zh_TW/header.vm")

以下是您在 POIPIKU 上未關注的一些推薦用戶。

#foreach($u in $recommend_users)
[$u.nickname]
#if($u.profile.length()>0)
$u.profile
#end
https://ai.poipiku.com/$u.userId

#end

跟隨 POIPIKU 不會通知其他人。隨時關注他們。

#parse("zh_TW/footer.vm")
