#parse("ja/header.vm")

あなたがポイピクでフォローしていないおすすめユーザーを紹介します。

#foreach($u in $recommend_users)
【$u.nickname】 #if($u.requestCreatorStatus==2) ☆リクエスト募集中 #end
#if($u.profile.length()>0)
$u.profile
#end
https://stg.poipiku.com/$u.userId

#end

ポイピクのフォローは相手に伝わりません。気軽にフォローしてみましょう。


※ メール配信のON/OFFは、ブラウザ版ポイピクの設定画面にて切り替えられます。
#parse("ja/footer.vm")
