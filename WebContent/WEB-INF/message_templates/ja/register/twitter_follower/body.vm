#parse("ja/header.vm")

あなたがTwitterでフォローしているユーザーがポイピクをはじめています。

#foreach($u in $recommend_users)
【$u.nickname】
#if($u.profile.length()>0)
$u.profile
#end
https://poipiku.com/$u.userId

#end

ポイピクのフォローは相手に伝わりません。気軽にフォローしてみましょう。

#parse("ja/footer.vm")
