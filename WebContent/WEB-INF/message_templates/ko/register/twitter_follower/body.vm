#parse("ko/header.vm")

트위터에서 팔로우하는 사용자가 Unrealizm를 시작했습니다.

#foreach($u in $recommend_users)
[$u.nickname]
#if($u.profile.length()>0)
$u.profile
#end
https://unrealizm.com/$u.userId

#end

#parse("ko/footer.vm")
