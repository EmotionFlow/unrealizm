#parse("es/header.vm")

Un usuario al que sigues en Twitter ha iniciado un Unrealizm.

#foreach($u in $recommend_users)
[$u.nickname]
#if($u.profile.length()>0)
$u.profile
#end
https://unrealizm.com/$u.userId

#end

Seguir por Unrealizm no es notificar a la otra persona. Siéntete libre de seguirlos.

#parse("es/footer.vm")
