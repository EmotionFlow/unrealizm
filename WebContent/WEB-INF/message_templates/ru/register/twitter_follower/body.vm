#parse("ru/header.vm")

Пользователь, на которого вы подписаны в Твиттере, создал Unrealizm.

#foreach($u in $recommend_users)
[$u.nickname]
#if($u.profile.length()>0)
$u.profile
#end
https://ai.poipiku.com/$u.userId

#end

Следование Unrealizm не является уведомлением другого лица. Не стесняйтесь следовать им.

#parse("ru/footer.vm")
